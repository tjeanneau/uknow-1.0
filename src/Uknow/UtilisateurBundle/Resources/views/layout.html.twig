<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        {% stylesheets
        '@bootstrap_css'
        'css/uknowConnexion.css'
        %}
        <link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
        {% endstylesheets %}
        <link rel="SHORTCUT ICON" href="{{ asset('images/logo.ico') }}">
        <link href='http://fonts.googleapis.com/css?family=Open+Sans:300,400&subset=greek' rel='stylesheet' type='text/css'>
        <title>{% block title %}{% endblock %}</title>
    </head>
    <body>
        <div class="espace1"></div>
        <div class="espace2">
            <h1 class="police3">Uknow</h1>
            <h2 class="police3">Knowledge is universal.</h2>
        </div>
        <div class="espace1"></div>
        <div class="espace2">

            <div class="container">
                <div class="row">
                    <div class="col-md-offset-4 col-md-4">
                        <div class="form-group" style="font-family: 'Open Sans', sans-serif; font-weight: 400">
                            {% block fos_user_content %}
                            {% endblock fos_user_content %}
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <footer id="footer">
            <h6>Staroads@{{ 'now'|date('Y') }}</h6>
        </footer>

        {% javascripts
        '@bootstrap_js'
        '@jquery'
        %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        {% endjavascripts %}
    </body>
</html>

{# src/Uknow/PlatformBundle/Resources/views/cartable/cours.html.twig#}

{% extends "UknowPlatformBundle::index.html.twig" %}

{% block title %}Mes cours{% endblock %}

{% block contenu %}
    <div class="row">
        <div class="col-md-11" style="padding: 0; position: fixed; z-index: 1" id="colLien">
            <ol class="breadcrumb">
                <li id="ajoutFooter" value="1" class="active">Mes cours</li>
            </ol>
        </div>
        <div class="col-md-offset-11 col-md-1" style="padding: 0; position: fixed; z-index: 1; background-color: #f5f5f5" id="colButton">
            <button id="agrandir" onclick="agrandir()">
                <span class="glyphicon glyphicon-resize-full"></span>
            </button>
        </div>
    </div>
    <div class="row" style="margin-top: 36px">
    <div id="content">
        <div class="col-md-offset-2 col-md-8" style="padding: 15px; text-align: center" id="colCours">
            {%  if listDonnees == null %}
                <h4>Vous n'avez pas enregistré de cours.<br/> Mais vous pouvez <em><a href="{{ path('uknow_platform_recherche_acceuil')}}">en trouver ici</a></em> !</h4>
            {% else %}
                {% for donnees in listDonnees %}
                    {{ include('UknowPlatformBundle:cartable:afficher.html.twig') }}
                    {{ include('UknowPlatformBundle:cartable:cacher.html.twig') }}
                {% endfor %}
            {% endif %}
        </div>
        </div>
    </div>
{% endblock %}